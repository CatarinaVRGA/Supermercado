docker run --rm -v ".:/app" composer update

docker compose up -d

Comandos GIT

adicionar um arquivo git add

adicionar todas as modificações git add .

realizar o commit git commit -m "<descrição do commit>"

realizar o push

git push origin

realizar o push no main git push origi main